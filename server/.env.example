# Port the HTTP + Socket.io server listens on. Railway sets PORT automatically.
PORT=5000

# Origin of the web client, used for CORS. In production use your Vercel URL,
# e.g. https://raja-mantri.vercel.app (no trailing slash).
CLIENT_URL=http://localhost:5173
